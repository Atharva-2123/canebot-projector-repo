# Runtime databases — machine data, never committed
*.db
*.db-shm
*.db-wal
*.log

# Device certificates are secrets
omega/certs/*.crt
omega/certs/*.key
omega/certs/*.pem

# Filled in per-device from omega-env.sh
omega/omega.env
